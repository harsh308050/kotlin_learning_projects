<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Base.Theme.MyDemo">
        <activity
            android:name=".users.UsersDetailsActivity"
            android:exported="false"
            android:theme="@style/Base.Theme.MyDemo" />
        <activity
            android:name=".users.UsersActivity"
            android:exported="false"
            android:theme="@style/Base.Theme.MyDemo" />
        <activity
            android:name=".home.HomescreenActivity"
            android:exported="false"
            android:theme="@style/Base.Theme.MyDemo" />
        <activity
            android:name=".auth.AuthActivity"
            android:exported="false"
            android:theme="@style/Base.Theme.MyDemo" />
        <activity
            android:name=".initial.GetStartedActivity"
            android:exported="false"
            android:theme="@style/Base.Theme.MyDemo" />
        <activity
            android:name=".initial.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.MyDemo.splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>