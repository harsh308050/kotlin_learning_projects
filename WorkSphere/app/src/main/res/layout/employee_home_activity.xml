<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:fitsSystemWindows="true"
    android:background="@color/white"
    android:layout_height="match_parent"
    tools:context=".employee.home.EmployeeHomeActivity">
    <FrameLayout
        android:id="@+id/fragmentContainer"
        android:layout_width="match_parent"
        android:layout_weight="1"
        android:layout_height="wrap_content" />
    <com.google.android.material.divider.MaterialDivider
        android:layout_width="match_parent"
        android:layout_height="0.5dp"
        android:layout_alignParentBottom="true"
        app:dividerColor="@color/light_grey" />
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:paddingHorizontal="10dp"
        android:paddingVertical="0dp"
        android:id="@+id/employee_bottom_nav"
        android:layout_width="match_parent"
        android:layout_height="75dp"
        android:layout_alignParentBottom="true"
        app:itemTextAppearanceActive="@style/BottomNavText"
        app:itemTextAppearanceInactive="@style/BottomNavText"
        android:background="@color/white"
        app:itemIconSize="25dp"
        app:itemActiveIndicatorStyle="@color/transparent"
        app:itemHorizontalTranslationEnabled="false"
        app:itemIconTint="@color/employee_bottom_nav_selector"
        app:itemRippleColor="@null"
        app:itemIconGravity="top"
        app:labelVisibilityMode="labeled"
        app:itemTextColor="@color/employee_bottom_nav_selector"
        app:menu="@menu/employee_bottom_nav_menu" />

</LinearLayout>